<h1>Añade una review for <%= @restaurant.name %></h1>

<%= simple_form_for [@restaurant, @review] do |form| %>
  <%= form.input :rating, collection: 0..5, as: :radio_buttons %>
  <%= form.input :content %>
  <%= form.button :submit, "Submit Review" %>
<% end %>
